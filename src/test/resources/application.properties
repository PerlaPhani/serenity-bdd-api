# Base test configuration — profile-specific files override api.base-url and server.port
# Default profile: local (set via pom.xml, override with -Dspring.profiles.active=dev)

api.connection-timeout=30000
api.read-timeout=30000
api.logging-enabled=true

spring.main.banner-mode=off
