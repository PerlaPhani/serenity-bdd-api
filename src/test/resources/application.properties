# Test configuration — local embedded server by default, override with -Dspring.profiles.active=dev
api.connection-timeout=30000
api.read-timeout=30000
api.logging-enabled=true

spring.main.banner-mode=off
