serenity {
  project.name = "Restful-API.dev — SerenityBDD API Test Suite"

  # Dump full request/response streams in the Serenity report
  rest.displayStreamDump = true

  # Capture screenshots/report details only when a test fails
  take.screenshots = FOR_FAILURES
}
